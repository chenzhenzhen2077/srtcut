# 产品升级方向

## 首页模式

首页先让用户选择目标：

- **智能剪辑**：上传视频后自动生成字幕、清理口癖，并生成 2-3 个内容片段方案供用户确认。
- **精细剪辑**：进入完整字幕时间轴，用户逐段检查、标记和调整剪辑。

智能剪辑的第一版流程：

```text
上传视频 -> 自动字幕 -> 基础清理 -> 生成 2-3 个候选方案 -> 用户选择 -> 渲染视频
```

候选方案必须先展示给用户确认，不直接自动导出最终视频。每个方案至少包含标题、摘要、Hook、起止时间、预计时长和置信度。

## AI 商业化

用户不需要理解 API Key、模型或供应商。产品提供统一的 AI 点数或套餐，服务端代为调用模型并记录用量。

推荐的第一版商业模型：

1. 免费体验额度，用于生成一次字幕或一个候选方案。
2. 充值 AI 点数，按视频分钟数或模型用量扣除。
3. 套餐中显示预计消耗和剩余额度，避免意外扣费。

必须遵守的边界：

- API Key 只能放在服务端环境变量或密钥管理服务中，不能下发到浏览器。
- AI 请求必须经过服务端代理、鉴权、限流、配额校验和成本记录。
- 记录供应商、模型、输入输出 token、视频时长、用户额度变化和请求状态。
- 明确告知用户视频/字幕是否会发送给第三方模型供应商，并提供本地识别选项。
- 支付接入前先完成用量统计和模拟扣费，避免先做支付再补计费逻辑。

## 技术阶段

1. 首页双模式入口和智能剪辑任务状态。
2. 候选内容片段数据结构和方案选择页面。
3. 服务端 AI Provider 抽象、用量记录和模拟额度。
4. 真实模型代理与成本保护。
5. 支付、充值和账单。

